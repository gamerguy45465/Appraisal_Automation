let _langchain_openai = require("@langchain/openai");
//#region src/utils/responses_stream_events.ts
async function* convertXAIResponsesStream(source, options = {}) {
	async function* mapped() {
		for await (const event of source) yield event;
	}
	yield* (0, _langchain_openai.convertOpenAIResponsesStream)(mapped(), {
		...options,
		provider: "xai"
	});
}
//#endregion
exports.convertXAIResponsesStream = convertXAIResponsesStream;

//# sourceMappingURL=responses_stream_events.cjs.map