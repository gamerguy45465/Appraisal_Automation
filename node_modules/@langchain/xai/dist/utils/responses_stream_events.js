import { convertOpenAIResponsesStream } from "@langchain/openai";
//#region src/utils/responses_stream_events.ts
async function* convertXAIResponsesStream(source, options = {}) {
	async function* mapped() {
		for await (const event of source) yield event;
	}
	yield* convertOpenAIResponsesStream(mapped(), {
		...options,
		provider: "xai"
	});
}
//#endregion
export { convertXAIResponsesStream };

//# sourceMappingURL=responses_stream_events.js.map